// Proctor helpers exposed to admin routes (e.g. listing violations).
// Currently the proctoring worker does the heavy lifting; this file is
// reserved for future server-side proctoring helpers.

module.exports = {};

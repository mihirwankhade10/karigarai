// Summary chain. Currently lives inside scoringChain (single LLM call returns
// scores + summaries together to keep cost down). This file is reserved for a
// future split if we want a richer summary independent of scoring.

module.exports = {};
